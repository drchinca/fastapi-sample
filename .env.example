# Optional locally. Required on Replit before connecting Marketo (Admin → Service Providers).

# MARKETO_USER=
# MARKETO_PASSWORD=

# Optional — overrides server URL in OpenAPI for /install and /openapi.json
# API_BASE_URL=https://your-app.replit.app
